import styled from 'styled-components';

const EventListStyle = styled.div`
  background: rgba(10, 20, 19, 0.2);
  width: 40%;
  max-height: 38rem;
  overflow: auto;
  padding: 1.6rem;
  button {
    margin-top: 1rem;
  }
  ul {
    margin-top: 1rem;
  }
  li {
    margin-top: 0.5rem;
  }
`;

export default EventListStyle;
